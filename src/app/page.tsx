import HomePageContent from "@/components/pages/HomePageContent";

export const metadata = {
  description:
    "Nexone Group is a diversified investment holding company connecting global capital with Africa's greatest opportunities across 11 strategic sectors.",
  alternates: { canonical: "/" },
};

export default function Home() {
  return <HomePageContent />;
}
